services:
  excel-analyzer:
    build: .
    container_name: excel-analyzer
    restart: always
    ports:
      - "5100:5000"
    environment:
      - TZ=Europe/Moscow
    volumes:
      - excel_analyzer_uploads:/app/uploads

volumes:
  excel_analyzer_uploads:
    external: false
